# Claude Certified Architect — 練習問題 進捗トラッカー

---

## ドメイン別 正答率サマリー

| ドメイン | 正解数 | 出題数 | 正答率 | 状態 |
|---------|--------|--------|--------|------|
| Agentic Architecture | 3 | 5 | 60% | 🟡 定着中 |
| Tool Design & MCP | 3 | 5 | 60% | 🟡 定着中 |
| Claude Code Configuration | 3 | 4 | 75% | 🟡 定着中 |
| Prompt Engineering | 3 | 4 | 75% | 🟡 定着中 |
| Context Management | 3 | 5 | 60% | 🟡 定着中 |
| Enterprise / Bedrock / Vertex | 2.5 | 4 | 63% | 🟡 定着中 |
| **合計** | **17.5** | **27** | **65%** | — |

> 凡例: 🔴 0〜49% / 🟡 50〜79% / 🟢 80〜100%

---

## セッション履歴

### Session #1 — 2026-07-17

**スコア: 0 / 5（0%）**

| 問 | ドメイン | あなたの答え | 正解 | 結果 |
|----|---------|------------|------|------|
| 1 | Agentic Architecture | B | A | ✗ |
| 2 | Tool Design & MCP | わからない | A | ✗ |
| 3 | Claude Code Configuration | C | B | ✗ |
| 4 | Prompt Engineering | C | B | ✗ |
| 5 | Context Management | C | B | ✗ |

**各問の解説**

**問1 — 正解: A**
> 各ステップが「前ステップの結果に依存」しているため、並列処理（B）は使えない。依存関係がある場合は単一エージェントの順次ツール呼び出しが正解。並列サブエージェント（B）は独立タスクに使う。

**問2 — 正解: A（`name`、`description`、`input_schema`）**
> Claude の Tool 定義で必須のフィールドは `name`・`description`・`input_schema` の 3 つ。`output_schema` は存在しない。`tool_choice` はツール定義ではなく API リクエストのパラメータ。

**問3 — 正解: B（PostToolUse フック）**
> 「必ず実行」= 自動強制。Hooks（PostToolUse）はファイル編集などのツール実行後に自動でコマンドを走らせる仕組み。Skills（C）はユーザーが手動で呼び出すもので、自動強制にはならない。

**問4 — 正解: B（XMLタグ + Few-shot）**
> Claude は XML タグで囲んだ構造定義と Few-shot 例の組み合わせで最も高精度な構造化抽出ができる。Extended Thinking（C）は推論には強いが出力フォーマットを保証しない。

**問5 — 正解: B（プロンプトキャッシュ）**
> コストと遅延の根本原因は「毎回同じ長いコンテキストを送り直すこと」。プロンプトキャッシュで system プロンプトや参照ドキュメントをキャッシュすれば、入力トークンコストを最大 90% 削減できる。ターン数制限（C）は情報を失うだけで根本解決にならない。

---

## 学習優先度マップ

### 🔴 優先度：高（全ドメイン）

セッション #1 の正答率が全ドメイン 0% のため、以下の順で重点学習を推奨します。

#### 1. Tool Design & MCP（問2：基礎知識が未定着）

問2は「わからない」と回答 → ツール定義の基本構造から確認が必要。

| 学習リソース | 内容 |
|------------|------|
| [ツール活用 概要](https://platform.claude.com/docs/en/docs/agents-and-tools/tool-use/overview) | ツールの仕組みの全体像 |
| [ツールの定義方法](https://platform.claude.com/docs/en/docs/agents-and-tools/tool-use/define-tools) | `name`/`description`/`input_schema` の構造 |

**確認すべきポイント:**
- [ ] ツール定義の必須フィールド 3 つを暗記する
- [ ] `tool_choice` がどこで使われるか理解する

---

#### 2. Claude Code Configuration（問3：Hooks vs Skills の混同）

Skills（手動）と Hooks（自動）の違いが曖昧。

| 学習リソース | 内容 |
|------------|------|
| [フック（Hooks）](https://code.claude.com/docs/en/hooks) | 自動実行の仕組み・トリガー種別 |
| [スキル（Skills）](https://code.claude.com/docs/en/skills) | スラッシュコマンドの作り方 |

**確認すべきポイント:**
- [ ] Hooks のトリガー種別（PreToolUse / PostToolUse など）を整理する
- [ ] 「自動強制」= Hooks、「手動呼び出し」= Skills という使い分けを定着させる

---

#### 3. Agentic Architecture（問1：順次 vs 並列の判断基準）

「依存関係がある → 順次」「独立している → 並列」の判断を誤った。

| 学習リソース | 内容 |
|------------|------|
| [サブエージェント](https://code.claude.com/docs/en/sub-agents) | 並列処理の設計パターン |
| [エージェント設計 概要](https://platform.claude.com/docs/en/docs/agents-and-tools/tool-use/overview) | オーケストレーターとサブエージェントの役割 |

**確認すべきポイント:**
- [ ] 順次処理が必要なケース vs 並列処理が有効なケースを例とともに説明できる

---

#### 4. Prompt Engineering（問4：構造化抽出の最適解）

Extended Thinking を過信している傾向あり。

| 学習リソース | 内容 |
|------------|------|
| [プロンプトエンジニアリング概要](https://platform.claude.com/docs/en/docs/build-with-claude/prompt-engineering/overview) | 構造化プロンプトの基本 |
| [Extended Thinking](https://platform.claude.com/docs/en/docs/build-with-claude/extended-thinking) | 適切なユースケースの確認 |

**確認すべきポイント:**
- [ ] XML タグによる出力フォーマット指定を実際に書いてみる
- [ ] Extended Thinking が向いているケース・向いていないケースを整理する

---

#### 5. Context Management（問5：プロンプトキャッシュの効果）

コスト削減の手段としてキャッシュではなくターン制限を選んだ。

| 学習リソース | 内容 |
|------------|------|
| [プロンプトキャッシュ](https://platform.claude.com/docs/en/docs/build-with-claude/prompt-caching) | キャッシュの仕組みとコスト削減効果 |

**確認すべきポイント:**
- [ ] キャッシュが効く条件（`cache_control` の書き方）を確認する
- [ ] 入力トークン削減 vs 品質低下（モデル変更）のトレードオフを整理する

---

### Session #2 — 2026-07-17

**スコア: 2 / 5（40%）**

| 問 | ドメイン | あなたの答え | 正解 | 結果 | 自信度 |
|----|---------|------------|------|------|--------|
| 1 | Agentic Architecture | C | B | ✗ | 自信なし |
| 2 | Tool Design & MCP | わからない | B | ✗ | — |
| 3 | Claude Code Configuration | B | B | ✓ | 自信あり |
| 4 | Prompt Engineering | B | B | ✓ | 自信あり |
| 5 | Context Management | わからない | B | ✗ | — |

**各問の解説**

**問1 — 正解: B（エラーをClaudeに返して判断させる）**
> Cは「コードがリトライを判断」という発想。エージェント設計の原則は「判断はClaudeに委ねる」。エラーをツール結果としてClaudeに返すことで、Claude自身がリトライ・代替手段・ユーザー通知を判断できる。

**問2 — 正解: B（`tool_result` コンテンツブロック）**
> `user` ロールで返すのは正しいが、中身の形式が間違い。`tool_use_id` を含む `tool_result` タイプのコンテンツブロックとして返す必要がある。
> ```python
> {"role": "user", "content": [
>     {"type": "tool_result", "tool_use_id": "toolu_xxx", "content": result}
> ]}
> ```

**問5 — 正解: B（静的コンテンツを先に置く）**
> キャッシュはプロンプト先頭からのprefixで効く。動的コンテンツ（user_question）が先頭に来ると、後続の静的コンテンツのキャッシュキーも毎回変わりヒットしない。「静的 → 動的」の順序が必須。

---

**観察: 自己評価の精度が高い**
> 自信あり（問3・4）= 全問正解、自信なし/わからない（問1・2・5）= 全問不正解。弱点の自覚が正確なため、優先学習範囲を絞りやすい。

---

## 学習優先度マップ（最新）

### 🔴 優先度：高

#### 1. Agentic Architecture — エラーハンドリング設計
**繰り返し間違えているパターン:** 「コードが判断する」vs「Claudeが判断する」

| 学習リソース | 内容 |
|------------|------|
| [ツール呼び出しの処理](https://platform.claude.com/docs/en/docs/agents-and-tools/tool-use/handle-tool-calls) | エラー時のツール結果の返し方 |
| [サブエージェント](https://code.claude.com/docs/en/sub-agents) | エージェントループの設計原則 |

**確認すべきポイント:**
- [ ] エラーはコードで吸収せずClaudeに渡す理由を説明できる
- [ ] 順次処理 vs 並列処理の判断基準（依存関係の有無）を定着させる

#### 2. Tool Design & MCP — tool_result の形式
**2セッション連続で「わからない」:** ツール呼び出しループの実装パターンが未定着

| 学習リソース | 内容 |
|------------|------|
| [ツール呼び出しの処理](https://platform.claude.com/docs/en/docs/agents-and-tools/tool-use/handle-tool-calls) | tool_result コンテンツブロックの構造 |

**確認すべきポイント:**
- [ ] `tool_use_id` が必要な理由を理解する（複数ツール同時呼び出し時の紐付け）
- [ ] ツール呼び出しループ全体の流れをコードで追える

#### 3. Context Management — キャッシュのprefix順序
**新しい弱点:** キャッシュが効く条件（コンテンツの順序）が未定着

| 学習リソース | 内容 |
|------------|------|
| [プロンプトキャッシュ](https://platform.claude.com/docs/en/docs/build-with-claude/prompt-caching) | cache_control の配置ルール |

**確認すべきポイント:**
- [ ] 「静的コンテンツ → 動的コンテンツ」の順序が必須な理由を説明できる
- [ ] cache_control を実際のコードで書ける

### 🟡 優先度：中

#### Claude Code Configuration / Prompt Engineering
2セッションで50%。正解しているが出題数が少ないため引き続き出題を続ける。

---

## 本日の学習まとめ（2026-07-17）

### 理解が深まった概念

**Hooks vs Skills（Claude Code Configuration）**
- Hooks = 自動強制（PreToolUse / PostToolUse でコマンドを自動実行）
- Skills = 手動呼び出し（`/コマンド名` でユーザーが任意に実行）
- 「必ず実行させる」要件 → 必ずHooks

**エージェントのエラーハンドリング（Agentic Architecture）**
- コードで判断（リトライ回数など）ではなく、エラーをツール結果としてClaudeに返してClaudeに判断させる
- Claudeの判断を安定させるにはsystem promptにルールを記述する（timeout→リトライ、権限エラー→終了、など）
- エラータイプによって適切な対処が異なるため、Claudeによる文脈判断が有効

**プロンプトキャッシュの順序（Context Management）**
- キャッシュはプロンプト先頭からのprefixで動作する
- 「静的コンテンツ（毎回同じ）→ 動的コンテンツ（毎回変わる）」の順序が必須
- 逆にすると毎回キャッシュキーが変わりヒットしない

**system promptとは**
- `client.messages.create()` の `system` パラメータ
- 会話全体に適用されるルール・指示を書く場所
- messagesのやり取りとは別に、Claudeの振る舞いを定義する

### 理解度が半分の概念（次回までに手を動かす）

- `tool_result` コンテンツブロックの形式（`tool_use_id` の役割）
- `cache_control` の書き方と配置

**原因:** APIを直接呼び出した経験がないため、リクエスト構造が実感として掴みにくい

---

## 次回セッションへのメモ

**Session #3 前にやること（推奨）**
- [ ] APIを1本動かす（`system` / `messages` / `stop_reason` の構造を実物で確認）
- [ ] ツール呼び出しループを動かす（`tool_result` を返す流れを体験する）
- 参考: 学習プランの「コツ1」と Phase 2 の curl サンプル

**Session #3 の出題方針**
- 🔴 ドメイン（Agentic Architecture・Context Management）を重点出題
- コードスニペット形式の問題を増やす（tool_result・cache_control の実装パターン）
- API経験後のスコア変化を確認する

---

### 穴埋めセッション — 2026-07-17 午後

**難易度：基礎〜中級 / 合計9問**

| セット | スコア | カバードメイン |
|--------|--------|--------------|
| セット1 | 1/3 | Tool Design、Context Management |
| セット2 | 3/3 | Tool Design、Agentic Architecture |
| セット3 | 3/3 | Prompt Engineering、Claude Code |

**穴埋めで定着した概念**
- ツール定義の必須3フィールド（`name` / `description` / `input_schema`）
- `tool_result` の構造（`role: user` / `type: tool_result` / `tool_use_id`）
- `tool_use_id` が必要な理由（複数ツール同時呼び出し時の紐付け）
- エラーはClaudeが読める文章で返す（Claudeに判断させるため）
- `<thinking>` タグでCoTを実装する
- CLAUDE.md に機密情報を書かない

**次回の穴埋め難易度について**
- 選択肢なしの自由記述を増やす
- 複数の概念を組み合わせた問題にする
- 正確なパラメータ名・値の記述を求める

---

---

## ハンズオンセッション — 2026-07-18

### 完了したこと
- 個人Anthropicアカウント作成・APIキー発行・環境変数設定
- `.claudeignore` / `CLAUDE.md` 作成
- `code/` フォルダ構造作成（01〜05）
- `step1_hello.py` — Messages API の基本構造を理解・実行
- `step2_tool_use.py` — Tool Use ループを実装・実行成功

### 理解が深まった概念
- `response` はAPIから返ってくるオブジェクト（自分で定義するものではない）
- `client.messages.create()` が呼ばれた瞬間がサーバーとの通信
- Tool Use は2往復（1回目でtool_use、2回目で最終回答）
- Claudeはツールを実行できない。「呼んでほしい」と指示するだけ
- ツール定義の `description` がClaudeの判断を左右する

### 今日理解が深まった概念
- `response` はAPIから返ってくるオブジェクト（自分で定義するものではない）
- `client.messages.create()` が呼ばれた瞬間がサーバーとの通信
- Tool Use は2往復（1回目でtool_use、2回目で最終回答）
- Claudeはツールを実行できない。「呼んでほしい」と指示するだけ
- ツール定義の `description` がClaudeの判断を左右する
- 複数ツール同時呼び出し時に `tool_use_id` で紐付けが必要な理由
- Claude はデフォルトで Markdown を返す。API では変換されないためそのまま見える
- `cache_control: {"type": "ephemeral"}` でキャッシュを有効化
- キャッシュは静的コンテンツ→動的コンテンツの順序が必須（プレフィックス照合のため）
- `cache_creation` / `cache_read` / `input_tokens` は独立したバケツ
- キャッシュ読み込みは通常の10分の1のコスト

### 作成したファイル
| ファイル | 内容 |
|---------|------|
| `code/01_message_api/step1_hello.py` | Messages API 基本 |
| `code/01_message_api/step3_cache.py` | プロンプトキャッシュ基本 |
| `code/01_message_api/step3_order.py` | 静的→動的の順序確認 |
| `code/02_tool_use/step2_tool_use.py` | Tool Use ループ |
| `code/02_tool_use/step2b_main.py` | ツール定義と実装の分離 |
| `code/02_tool_use/step2c_main.py` | 現在日時ツール（本質的な例）|

### 次回やること
- [ ] `step4_error.py` の発展版 — system prompt にルールを書いてエラーハンドリングを制御する
- [ ] MCQ Session #3（ハンズオン後のスコア変化を確認）

---

## ハンズオンセッション — 2026-07-22

### 完了したこと
- `code/03_agents/file_tool.py` — ファイル読み込みツール（良い実装 vs 悪い実装）
- `code/03_agents/step4_error.py` — エラーハンドリングの比較実行

### 理解が深まった概念
- エラーを空文字で返すと Claude が原因を判別できない（空=空ファイル？空=存在しない？）
- エラーを文字列で返すと Claude が正確に判断できる
- 複数ツール・複数ステップのループでは最初のエラーが後続判断をすべてずらす
- system prompt にルールを書くことで Claude のエラー時の行動を制御できる（次回実装）
- VS Code スニペットに `tool-def` / `msg-create` / `msg-create-tools` を登録

---

## ハンズオンセッション — 2026-07-23

### 完了したこと
- `step4b_error_with_system.py` — system prompt でエラー制御・while True ループ・MAX_ITERATIONS 実装

### 理解が深まった概念
- `stop_reason` は「停止理由」ではなく「返答を区切った理由」
- `while True` ループで Claude が `end_turn` を返すまで繰り返す
- 永久ループ対策として `MAX_ITERATIONS` をコード側で設ける
- 上限到達時は Claude に通知して最終報告を生成させる
- 「ユーザーが書いたコード = ユーザーがコントロール」「判断 = Claude がコントロール」の分担

### MCQ Session #3 結果
**スコア: 5/5（100%）** ← ハンズオン効果が明確に出た（前回53%→今回100%）

| 問 | ドメイン | 結果 |
|----|---------|------|
| 1 | Agentic Architecture | ✅ |
| 2 | Agentic Architecture | ✅ |
| 3 | Context Management | ✅ |
| 4 | Tool Design & MCP | ✅ |
| 5 | Context Management | ✅ |

### 次回やること
- [x] MCP 接続（Phase 4 完了）
- [x] Phase 5（Claude Code 設定・Agent SDK）開始

---

## ハンズオンセッション — 2026-07-24

### 完了したこと
- `code/05_claude_code/specialist.py` — サブエージェント定義 + `run()`（Claude API 呼び出し）
- `code/05_claude_code/step1_orchestrator.py` — オーケストレーターループ（ツール呼び出し → サブエージェント実行）
- 動作確認済み（天気・料理・地理・海洋生物学など複数の質問で確認）

### 理解が深まった概念
- **サブエージェントの実体**：`run()` の中身が Claude API 呼び出しになっているだけ。Phase 2 の `weather_tool.run()` と構造は同じ
- **`block` の正体**：`for block in response.content:` のループ変数。`response.content` はAPIから返るブロックのリスト
- **`block.input`**：Claude が `input_schema` に従って自律的に埋めた値（`specialty`、`question`）
- **オーケストレーターによる質問の変換**：ユーザーの口語をオーケストレーター Claude が「専門家向けの質問」に書き直してから `block.input["question"]` に詰める。`description` の文言が判断を左右する
- **今回の例の限界**：単純な Q&A はオーケストレーター単独でも答えられる。サブエージェントの真価は「並列処理」「専用ツール持ち」「異なるモデル」のときに出る

### 作成したファイル
| ファイル | 内容 |
|---------|------|
| `code/05_claude_code/specialist.py` | サブエージェント定義 + run()（Claude API） |
| `code/05_claude_code/step1_orchestrator.py` | オーケストレーターループ |

### 追記（同日）
- `step2_parallel.py` — 順次処理 vs 並列処理（6.8秒 → 3.9秒）、動作確認済み

### MCQ Session #5 結果
**スコア: 2/5**

| 問 | ドメイン | 結果 | メモ |
|----|---------|------|------|
| 1 | Agentic Architecture | △ | 方向は正しいが「ローカル処理 vs API呼び出し」の対比が不明確 |
| 2 | Agentic Architecture | △ | Claudeが決めるは○、input_schema の description に基づく点が未回答 |
| 3 | Agentic Architecture | ✗ | Future オブジェクトの説明がハンズオンで未実施（再学習予定） |
| 4 | Agentic Architecture | ✓ | as_completed() は完了順に返る |
| 5 | Agentic Architecture | △ | 時間メリットは○、「タスクの独立性」が条件という点が未言及 |

### 理解が深まった概念
- `ThreadPoolExecutor` で複数タスクを同時実行できる
- `executor.submit()` は即座に `Future` オブジェクト（番号札）を返す
- `future.result()` は処理完了まで待ち、関数の戻り値を返す
- `as_completed()` は完了した順にイテレートする（投げた順ではない）
- 並列処理が使える条件：タスクが互いに**独立**していること

### 再学習が必要な概念
- `concurrent.futures` 全体（`Future` オブジェクト、辞書内包表記のパターン）

### 次回やること
- [ ] concurrent.futures セクションを再学習
- [ ] Phase 6（エンタープライズ・Bedrock/Vertex）へ進む

---

---

## ハンズオンセッション — 2026-07-31

### 完了したこと
- AWS CLI インストール・`aws configure` 設定（~/.aws/credentials）
- `code/06_enterprise/step1_bedrock.py` — Bedrock 経由で Claude 呼び出し成功
- `code/06_enterprise/step2_vertex.py` — Vertex AI 参考コード作成（GCP 未設定のため実行なし）

### 理解が深まった概念
- **3つのクライアントクラス**：`Anthropic()` / `AnthropicBedrock(aws_region=...)` / `AnthropicVertex(region=..., project_id=...)`
- **モデルID 形式の違い**（試験頻出）：
  - Anthropic 直接: `claude-haiku-4-5-20251001`
  - Bedrock: `us.anthropic.claude-haiku-4-5-20251001-v1:0`（`us.` 推論プロファイル + `-v1:0` 接尾辞）
  - Vertex AI: `claude-haiku-4-5@20251001`（`@` 区切り、接頭辞・接尾辞なし）
- **認証方式の違い**：直接API = `ANTHROPIC_API_KEY` 環境変数 / Bedrock = `~/.aws/credentials` / Vertex = GCP ADC
- **引数なし = ローカル環境変数を自動読み込み**：コードに認証情報を書かないセキュリティベストプラクティス
- **推論プロファイル**：新しい Bedrock モデルは `us.` プレフィックスが必要
- **なぜ Bedrock/Vertex を使うか**：データが自社クラウド内に留まる・IAM でアクセス管理・コスト一元管理
- **GCP プロジェクト vs AWS アカウント**：GCP はアカウントより下の階層にプロジェクトがある → `project_id` の明示指定が必要
- **`messages.create()` の中身は3つとも同じ**：接続先が変わっても既存コードがそのまま動く

### 作成したファイル
| ファイル | 内容 |
|---------|------|
| `code/06_enterprise/step1_bedrock.py` | Bedrock クライアント + 動作確認 |
| `code/06_enterprise/step2_vertex.py` | Vertex AI 参考コード（実行なし） |

### 次回やること
- [x] エンタープライズ設計トピック（セキュリティ・コスト管理）— 完了
- [x] MCQ Session #6 — 完了
- [ ] concurrent.futures 再学習（ペンディング）
- [ ] 英語MCQ総復習（全フェーズ対象・本番形式）

---

## ハンズオンセッション — 2026-07-31（Phase 7: Webアプリ）

### 完了したこと
- Flask インストール
- `code/07_webapp/app.py` — Flask サーバー + ツールループ
- `code/07_webapp/my_tools.py` — 直接Tool（datetime・weather）+ MCPツール定義（`my_mcp_tools`）
- `code/07_webapp/templates/index.html` — フロントエンド（textarea・送信・ツール表示）
- `code/07_webapp/MCP_server/converter_server.py` — MCPサーバー（摂氏↔華氏変換）
- 動作確認済み（全ツール呼び出し確認）

### 理解が深まった概念
- **FlaskのWebアプリ構成**：`@app.route` でURLとPython関数を紐付ける
- **フロントエンドとバックエンドの通信**：JavaScript `fetch()` → Flask POST → JSONレスポンス
- **ツールループの位置**：Phase 2-3 で書いたループがそのまま Flask の中に入る
- **Tool vs MCP の実態**：関数の中身は同じ。MCPプロトコルはClaude Code専用、WebアプリはPython importで関数を再利用
- **my_tools / my_mcp_tools の分離**：出所をコードで明示する設計
- **ツール選別の重要性**：ツールが増えるとトークン増加・判断精度低下のリスク

### 作成したファイル
| ファイル | 内容 |
|---------|------|
| `code/07_webapp/app.py` | Flask サーバー・ツールループ・ログ出力 |
| `code/07_webapp/my_tools.py` | Tool定義・MCP Tool定義・run_tool() |
| `code/07_webapp/templates/index.html` | フロントエンド（JS fetch含む） |
| `code/07_webapp/MCP_server/__init__.py` | Pythonパッケージ宣言（空ファイル） |
| `code/07_webapp/MCP_server/converter_server.py` | 温度変換MCPサーバー |

### 次回やること
- [ ] Toolのみで実現できる機能：インメモリメモ保存・取得（Flask状態依存）
- [ ] Tool と MCP の違いを体験で理解する
- [ ] concurrent.futures 再学習（ペンディング）
- [ ] 英語MCQ総復習

*最終更新: 2026-09-01 | MCQセッション数: 6 | 穴埋めセット数: 3 | ハンズオンセッション数: 7 | 累計出題数: 27*

---

## MCQ Session #6 — 2026-07-31

**スコア: 3.5 / 5（70%）**

| 問 | ドメイン | 結果 | メモ |
|----|---------|------|------|
| 1 | Enterprise / Bedrock | △ | `aws_region`・`us.`プレフィックスは正解。クラス名 `AnthropicBedrock` が誤り |
| 2 | Enterprise / Vertex | ✓ | `region` / `project_id`（typoあり、概念は正解） |
| 3 | Enterprise / Cost | ✗ | Batch API = 約50%削減と答えるべきところを「変わらない」と誤答 |
| 4 | Enterprise / Security | ✓ | Bedrock/Vertexはレイテンシ改善が目的ではない |
| 5 | Claude Code Configuration | ✓ | 組織→プロジェクト→ユーザーの3層構造 |

**各問の解説**

**問1 — 正解: `AnthropicBedrock` / `aws_region` / `us.`**
> クラス名3つは試験頻出。`Anthropic()` / `AnthropicBedrock(aws_region=...)` / `AnthropicVertex(region=..., project_id=...)` を正確に区別する。

**問3 — 正解: C（約50%削減）**
> Message Batching = 非同期（遅延）処理 → 最大24時間後に結果。リアルタイム応答が不要なユースケース（ログ分析・大量ドキュメント要約など）で有効。**Batch = 安いけど後回し**。

**定着した概念**
- Batch API = 50%割引、結果は最大24時間後（遅延処理）
- 通常の非同期（asyncio）= クライアント側が並行制御 → レイテンシ改善
- Batch の非同期 = サーバー側が後回し → レイテンシ悪化・コスト削減
- エンタープライズでBedrock/Vertexを使う理由：データ保護・IAM統合・統合請求
- 設定の3層：組織（上書き不可）→ プロジェクト（CLAUDE.md）→ ユーザー

**要復習**
- 3クライアントクラスの正確な名前とパラメータ名

---

*最終更新: 2026-09-01 | MCQセッション数: 6 | 穴埋めセット数: 3 | ハンズオンセッション数: 6 | 累計出題数: 27*

---

## ハンズオン TODO（未実施）

| # | テーマ | Phase | ファイル案 | 状態 |
|---|--------|-------|-----------|------|
| 1 | Streaming（ストリーミング応答） | Phase 1 補足 | `code/01_message_api/step4_streaming.py` | [x] 完了 |
| 2 | Parallel Tool Use + tool_choice | Phase 2 発展 | `code/02_tool_use/step3_parallel_tools.py` | [x] 完了 |
| 3 | Vision（画像入力） | Phase 2 発展 | `code/02_tool_use/step4_vision.py` | [x] 完了 |
| 4 | Batch API（非同期バッチ処理） | Phase 1 補足 | `code/01_message_api/step5_batch.py` | [x] 完了 |
| 5 | concurrent.futures 再実装 | Phase 5 再学習 | `code/05_claude_code/step2b_futures.py` | [ ] 未着手 |

---

## 英語MCQ総復習 — 第4回（2026-09-01）

**スコア: 3 / 3（100%）**

| 問 | テーマ | 結果 | キーポイント |
|----|--------|------|-------------|
| Q13 | プロンプトキャッシュ（繰り返し文書参照） | ✅ | system + cache_control。Batch APIは非同期のためリアルタイム不可 |
| Q14 | MCP vs インラインツール、Batch API適用場面 | ✅ | 再利用不要→インラインで十分。Batch API = 非同期 = リアルタイム不可 |
| Q15 | プロンプトインジェクション根本対策 | ✅ | `<untrusted_content>` タグ + system で境界分離。モデルの安全訓練に依存NG |

**英語MCQ総計: 13/15（87%）**

---

### 各トピックのポイント

**1. Streaming**
- `with client.messages.stream(...) as stream:` の構文
- `stream.text_stream` でトークンをリアルタイム受信
- ストリーミングが有効なユースケース（UX改善・長文生成）

**2. Parallel Tool Use + tool_choice**
- Claudeが1レスポンスで複数の `tool_use` ブロックを返すケース
- 全ブロックに `tool_result` を返す必要がある（1つでも欠けるとエラー）
- `tool_choice: {"type": "auto" | "any" | "tool", "name": "..."}` の使い分け

**3. Vision（画像入力）**
- `content` にテキストと画像を混在させる構造
- Base64エンコード or URL 指定の2方式
- `"type": "image"` / `"source": {"type": "base64", "media_type": "...", "data": "..."}` の形式

**4. Batch API**
- `client.messages.batches.create(requests=[...])` でバッチ送信
- 最大24時間後に結果取得（非同期）
- コスト約50%削減・リアルタイム不要のユースケース向け

**5. concurrent.futures 再実装**
- `Future` オブジェクトの仕組み（番号札 → `result()` で受け取る）
- `{future: label for future, label in zip(futures, labels)}` 辞書内包表記
- `as_completed()` は完了した順にイテレート（投げた順ではない）
